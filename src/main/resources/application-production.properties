# Production Configuration for Render
spring.application.name=diepxdemo

# Database Configuration for Production - MySQL
spring.datasource.url=${DATABASE_URL:jdbc:mysql://137.59.105.34:3306/z2jcas1wtfi5_tonton?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DATABASE_USERNAME:z2jcas1wtfi5_ton}
spring.datasource.password=${DATABASE_PASSWORD:=cQVf#~%R[xc}
spring.datasource.driver-class-name=${DATABASE_DRIVER:com.mysql.cj.jdbc.Driver}

# JPA/Hibernate Configuration for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.MySQLDialect}
spring.jpa.properties.hibernate.format_sql=false

# Thymeleaf Configuration for Production
spring.thymeleaf.cache=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# Server Configuration (Render uses dynamic PORT)
server.port=${PORT:10000}

# Logging Configuration for Production
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.root=INFO

# Security Configuration
server.error.whitelabel.enabled=false

# Application specific settings
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# Render specific configurations
server.tomcat.threads.max=200
server.tomcat.max-connections=10000
