# Production Configuration for Render
spring.application.name=diepxdemo

# Database Configuration for Production - MySQL
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://dpg-d2nvhf3e5dus739gs09g-a.oregon-postgres.render.com:5432/tonton_dtfs?sslmode=require}
spring.datasource.username=${DATABASE_USERNAME:tonton_dtfs_user}
spring.datasource.password=${DATABASE_PASSWORD:B4gObzp1u9jgdiRjVmhKOGRXXPb32CdJ}
spring.datasource.driver-class-name=${DATABASE_DRIVER:org.postgresql.Driver}

# JPA/Hibernate Configuration for Production
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=${HIBERNATE_DIALECT:org.hibernate.dialect.PostgreSQLDialect}
spring.jpa.properties.hibernate.format_sql=false

# Thymeleaf Configuration for Production
spring.thymeleaf.cache=true
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

spring.jackson.time-zone=Asia/Ho_Chi_Minh
spring.jpa.properties.hibernate.jdbc.time_zone=Asia/Ho_Chi_Minh


# Server Configuration (Render uses dynamic PORT)
server.port=${PORT:10000}

# Logging Configuration for Production
logging.level.org.springframework.web=INFO
logging.level.org.hibernate.SQL=WARN
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=WARN
logging.level.root=INFO

# Security Configuration
server.error.whitelabel.enabled=false

# Application specific settings
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when_authorized

# Render specific configurations
server.tomcat.threads.max=200
server.tomcat.max-connections=10000
